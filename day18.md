Day 18 - Environment Variables and Secrets

Today I learned:
- Why sensitive data (passwords, keys) should never be written directly in code
- How to create a GitHub Secret
- How to use a secret safely in a workflow (env + secrets context)
- Difference between a secret (hidden in logs) and a regular environment variable (visible in logs)
- Debugged a YAML indentation error that prevented the workflow from showing up

