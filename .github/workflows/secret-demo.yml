name: Secret Demo

on: push

jobs:
  show-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secret safely
        env:
          MY_SECRET: ${{ secrets.MY_SECRET_MESSAGE }}
        run: echo "The secret has been loaded (but won't be shown in logs for safety)"
