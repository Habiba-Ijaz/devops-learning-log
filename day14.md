Day 14 - Docker Networking

Today I learned:
- Why containers can't talk to each other by default
- What a Docker network is
- How to create a network (docker network create)
- How to connect containers to a network (--network flag)
- Confirmed containers can find each other by name when on the same network
